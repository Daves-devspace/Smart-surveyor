# Create a projects/services.py (or projects/tasks.py) and implement functions:

# enqueue_generate_ai_layout(project_id, user_id) → enqueues Celery

# generate_ai_layout_sync(project_id) → local algorithm for testing

# validate_project_ready_for_submission(project) → returns list of problems or empty lis